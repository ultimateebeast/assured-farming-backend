# core framework
Django>=4.2,<6

# REST / API / docs
djangorestframework>=3.16
drf-spectacular>=0.29

# env & config
django-environ>=0.10.0
python-dotenv>=1.0

# auth
djangorestframework-simplejwt>=5.0

# CORS, static files & WSGI
django-cors-headers>=4.0
whitenoise>=6.0
gunicorn>=20.1

# Postgres & DB helpers
psycopg2-binary>=2.9
dj-database-url>=1.0

# Celery / Redis (if you use them)
celery>=5.0
redis>=4.0

# logging / utilities
python-json-logger>=2.0

# Optional: forms/templating you used in code
django-crispy-forms>=2.0

# Keep these at the end to avoid duplicates during automated parsing
# pin minimal commonly-required libs (no heavy dev / windows-only)
